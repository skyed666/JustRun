# JustRun — 功能规格（闭环对照）

本文是实现与验收清单。每条有 ID。状态：`[ ]` 未完成 / `[x]` 已落地。

**明确不做：** Magisk / LSPosed / 隐藏模拟器 / 改指纹 / 绕过购物 App 风控。

**闭环定义：** 入口 → 操作 → 状态反馈（状态栏 / 进度 / 结果）→ 可回看或跳转相关页。

对照日期：2026-08-25。对照以代码行为为准。

---

## 0. 总原则

| ID | 要求 | 状态 | 对照 |
|---|---|---|---|
| G-01 | 工具不可用时禁用相关按钮，并给出原因 | [x] | Docker/ADB 页 `disabled` + `title` |
| G-02 | 危险操作（删容器/卷/停机）需确认 | [x] | Docker 删、Volumes 删、批量停止 |
| G-03 | 列表筛选/搜索离开再回仍在（session） | [x] | devices / docker / volumes / logs / apk |
| G-04 | 复制类操作成功写状态栏 | [x] | Serial / 卷名 / 日志 / inspect |
| G-05 | 在线 = `online && adbStatus === "device"` | [x] | StatusBar、Devices、Apk、侧栏 |

---

## 1. Dashboard

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| D-01 | Docker / ADB / 在线数可点跳转 | [x] | `Dashboard.tsx` StatCard `onClick` |
| D-02 | 设备预览最多 6 台，点行进详情 | [x] | slice(0,6) + 查看全部 |
| D-03 | 无设备 CTA「去创建」 | [x] | navigate `/docker` |
| D-04 | 通知可点击跳转 | [x] | Docker/ADB/设备/日志 |
| D-05 | 最近日志点行带筛选进日志页 | [x] | session `rdc.logs.*` |
| D-06 | 最近截图点文件名打开所在目录 | [x] | revealInFolder |
| D-07 | 最近 APK 点文件名预填路径并跳转 | [x] | `rdc.apk.path` → `/apk` |
| D-08 | 正在投屏台数，点进设备中心 | [x] | `scrcpyStatus === "running"` |

---

## 2. 设备中心

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| V-01 | 搜索/筛选记住 | [x] | session keys |
| V-02 | 多选、全选、复制 Serial | [x] | |
| V-03 | 批量连接/装包/截图/断开/重启/停止/亮屏/锁屏/HOME/BACK/RECENT；结果可点进详情、只选失败 | [x] | 结果清单 |
| V-04 | 卡片进详情、投屏、更多 | [x] | |
| V-05 | 刷新失败写状态栏 | [x] | `load` catch |

---

## 3. 设备详情

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| E-01 | 打开时离线自动等开机并连 ADB | [x] | `connectIfNeeded` |
| E-02 | 顶栏启动/停止/重启/连接 | [x] | |
| E-03 | Tab 记住 | [x] | `rdc.detail.tab.*` |
| E-04 | 离线禁用控制/文件/应用，Logcat 停 | [x] | 「需在线」 |
| E-05 | 概览复制；数据卷/容器跳转 | [x] | session 预填搜索 |
| E-06 | 控制 + scrcpy | [x] | |
| E-07 | 文件书签/上传下载 | [x] | |
| E-08 | 应用启停清数据卸载安装 | [x] | |
| E-09 | Logcat 筛选/暂停；导出走保存对话框并可打开目录 | [x] | |
| E-10 | 分辨率/DPI/语言、自动启动 | [x] | |

---

## 4. Docker

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| K-01 | 工具条，不可用禁用操作 | [x] | |
| K-02 | WSL binder 切换/检测 | [x] | |
| K-03 | 名称/端口占用与建议 | [x] | |
| K-04 | 创建阶段 + 秒数 + state + boot；可勾选是否等 ADB | [x] | `get_create_stage` + `waitAdbPref` |
| K-05 | 失败保留进度，「打开这台」 | [x] | |
| K-06 | 留在表单连建，不抢滚动 | [x] | |
| K-07 | 创建后加入自动启动 | [x] | |
| K-08 | 实例搜索记住 + 绿框 | [x] | |
| K-09 | 启停重启克隆重命名导出删除 | [x] | |
| K-10 | inspect 弹层可复制 | [x] | `inspect_container` |
| K-11 | 容器日志弹层复制/刷新 | [x] | `get_container_logs` |
| K-12 | 复制实例 ADB Serial | [x] | |
| K-13 | 镜像搜索/删除/清理悬空 | [x] | |

---

## 5. 数据卷

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| O-01 | 搜索/筛选记住；显示大小（悬停看挂载点） | [x] | |
| O-02 | 点卷名复制 | [x] | |
| O-03 | 打开设备 | [x] | |
| O-04 | 打开实例（预填 Docker 搜索） | [x] | `rdc.docker.instQuery` |
| O-05 | 删除/清理空闲卷需确认 | [x] | |
| O-06 | 其它卷只读 | [x] | |

---

## 6. ADB

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| A-01 | 版本/Server/设备数 | [x] | |
| A-02 | 记住地址，Enter 连接；「打开这台」进详情 | [x] | |
| A-03 | Server 维护、自动修复、断开全部 | [x] | |
| A-04 | 点 Serial 复制；行内重连/断开 | [x] | |
| A-05 | 工具不可用禁用 | [x] | |

---

## 7. APK

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| P-01 | 路径/覆盖记住 | [x] | |
| P-02 | 搜索、全选在线、离线变淡 | [x] | |
| P-03 | 离线安装确认 | [x] | |
| P-04 | `完成 x/n`；结果表可点设备、只选失败、复制/清空 | [x] | |
| P-05 | 无设备去创建 | [x] | |
| P-06 | Dashboard 点 APK 路径已填 | [x] | `rdc.apk.path` |

---

## 8. 日志

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| L-01 | 源/级别/关键字记住 | [x] | |
| L-02 | 打开目录、清空 | [x] | |
| L-03 | 复制行、导出筛选 | [x] | |
| L-04 | 暂停刷新按钮 + 滚动暂停 + 仅 ERROR | [x] | 顶栏「暂停刷新」 |

---

## 9. 设置

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| S-01 | 主题保存生效；侧栏切换也会写入设置 | [x] | Settings + Sidebar `saveSettings` |
| S-02 | 路径浏览/打开/检测 | [x] | |
| S-03 | 自动启动清单清理 | [x] | |
| S-04 | 创建默认：自动启动 / 留在表单 / 等待 ADB | [x] | |
| S-05 | 语言写 `html lang`；代理保存后作用于 docker/adb 子进程；自动更新仅回显 | [x] | 不接外部更新器 |

---

## 10. 布局 / 自动启动

| ID | 功能 | 状态 | 对照 |
|---|---|---|---|
| X-01 | 侧栏 Devices 显示 ADB 就绪台数 | [x] | Sidebar 角标，`online && adbStatus === "device"` |
| X-02 | 状态栏可点，0 设备 CTA | [x] | StatusBar |
| X-03 | 开机自动启动最多 2 并发 | [x] | AppLayout `runAutoStart` |
| X-04 | 15s 轮询，隐藏暂停，失败计数 | [x] | visibility + appStore |

---

## 本轮实现（B1）

- 后端：`get_container_logs`（`docker logs --tail`）
- Dashboard：卡片跳转、投屏计数、APK 预填路径
- Docker：检查 / 日志 / 复制 Serial
- Volumes：打开实例
- 详情：打开容器 + 数据卷带搜索
- APK：无设备去创建
- Devices：刷新失败状态栏

**文档条目已全部勾选。** 容器日志/检查需重跑 `npm run tauri dev` 才能调到新命令。

## 本轮实现（B23 · Logcat / 内核切换）

- Logcat 拉取失败写进输出区
- WSL 内核切换 / 检测失败走 `run()` 错误提示，不再假装成功

## 本轮实现（B22 · 列表刷新失败可见）

- 设备详情、ADB、文件、应用列表刷新失败写状态栏

## 本轮实现（B21 · 编译补全 + APK/日志）

- 成功截图补上 `error` 空字段，避免结构体缺字段
- APK 页拉设备失败写状态栏
- 系统日志切到后台暂停轮询

## 本轮实现（B20 · 截图原因 + 离开确认）

- 截图失败带回端错误信息
- 设置未保存时路由离开会确认（Hash 内跳转）

## 本轮实现（B19 · 日志/截图失败可见）

- 系统日志刷新、清空失败写状态栏
- 详情截图失败弹提示

## 本轮实现（B18 · 导出目录 + 关于版本）

- 导出容器 JSON 看成败，成功可打开所在目录
- 关于页版本从 Tauri `getVersion()` 读取
- 设置未保存时可「放弃更改」

## 本轮实现（B17 · 克隆定位 + 设置脏提示）

- 克隆成功后搜索并高亮新实例
- 设置有未保存更改时提示，保存按钮在未改时禁用

## 本轮实现（B16 · Docker 操作看成败）

- 实例启停/重启/克隆等 `run()` 会检查 `ShellResult.success`，失败不再显示「就绪」
- 数据卷刷新失败写状态栏

## 本轮实现（B15 · 保存/刷新失败可见）

- 设置保存失败弹原因，不再静默
- Dashboard 刷新失败写状态栏；切到后台暂停轮询

## 本轮实现（B14 · ADB 操作反馈）

- ADB 连接 / Server 启停重连 / 修复 / 断开全部：看成败，危险操作先确认
- 行内重连/断开写状态栏
- 创建成功时把「等 ADB」写入设置

## 本轮实现（B13 · 文件操作成败 + 诚实更新开关）

- 文件新建/上传/下载/删除检查返回值，失败弹原因；下载成功可打开目录
- 设置「自动更新」标明本版不会检查更新
- 浏览 APK 后把所在目录写入设置 `apkPath`

## 本轮实现（B12 · 代理生效 / 去掉假端口）

- 设置代理保存后写入子进程 `HTTP(S)_PROXY`（拉镜像会走代理）
- 创建表单去掉无效的 Scrcpy 端口，改为说明投屏走 ADB
- 数据卷「打开目录」尝试打开挂载点（WSL 路径可能打不开，会提示）

## 本轮实现（B11 · 等 ADB 默认 + 卷大小）

- 设置新增「默认等待 ADB」；创建表单勾选会写入设置
- 数据卷列表显示大小，悬停看挂载点（`docker system df -v`）

## 本轮实现（B10 · 等 ADB / 打开这台 / Logcat 导出）

- 创建表单可勾选「等待 ADB 开机」，关掉则只建容器（记住选择）
- ADB 页连接框旁「打开这台」
- 详情 Logcat 导出改为系统保存对话框，并可打开所在目录

## 本轮实现（B9 · APK 结果与容器入口）

- E-10：去掉无效「容器参数」预留，改为说明 +「打开该实例」
- P-04：安装结果改成表，点设备进详情，失败可「只选失败」

## 本轮实现（B8 · 记忆与批量）

- 详情面板开关、上次选中设备写入 localStorage，刷新仍在
- 批量结果「只选失败」，方便重试
- 批量增加锁屏（已有 `device_lock`）

## 本轮实现（B7 · 批量/刷新/语言）

- V-03：批量结果点设备名进详情
- Docker 刷新失败写状态栏
- S-05：设置语言保存后应用到 `html lang`

## 本轮实现（B6 · 详情与 Docker 列表）

- 文件：筛选无结果「清除筛选」
- 应用：无应用提示安装；搜索无结果「清除搜索」
- Logcat：过滤无结果「清除过滤」
- Docker 全部容器：点 Redroid 名称定位到实例列表
- 镜像：搜索无结果「清除搜索」

## 本轮实现（B5 · 空状态闭环）

- Volumes：无卷「去创建」；筛选空「清除筛选」
- ADB：无设备「去创建 / 设备中心」
- 设置：自动启动空清单「去设备中心」
- APK：搜索无结果「清除搜索」
- Dashboard：无截图「去截图」；无 APK「去安装」；无通知「打开日志」
- 日志：筛选无结果「清除筛选」

## 本轮实现（B4 · 对照后补漏）

- V-03：批量结果可「复制结果」
- 设备中心：无设备「去创建 / 去 ADB」；筛选为空可清除；卡片卷名带搜索跳转
- Docker：无实例「去创建」；筛选为空可清除
- ADB：设备行「打开」进详情

## 本轮实现（B3 · 对照后补漏）

- L-04：日志页增加明确的「暂停刷新 / 继续刷新」（不只靠滚动）
- K-09：实例「数据卷」预填卷名再跳转
- 右侧详情：Docker / ADB / 在线数可点；Serial 可复制；无设备「去创建」
- G-05：Dashboard 在线数改用设备列表的 ADB device，不再用后端 ADB 扫描计数

## 本轮实现（B2 · 对照后补漏）

- X-01 原先只写在文档里：侧栏 Devices 现显示 ADB 就绪台数
- S-01：侧栏切主题会 `saveSettings`，重启仍在
- K-09~K-12：实例行「更多」收纳复制 Serial / 检查 / 日志 / 克隆 / 重命名 / 导出，避免按钮挤爆
