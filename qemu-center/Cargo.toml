[package]
name = "qemu-center"
version = "0.1.0"
edition = "2021"
description = "Standalone QEMU/WHPX track for JustRun: one VM = one node hosting N redroid containers"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
dirs = "6"
base64 = "0.22"
ed25519-dalek = "2"

[dev-dependencies]
# Intentionally empty: tests use the same runtime dependencies as the CLI.

[lib]
name = "qemu_center"
path = "src/lib.rs"

[[bin]]
name = "qemu-center"
path = "src/main.rs"
